type: pkg

project:
  name: canbus
  version: 0.0.1
  keywords:
  - wio
  - pkg
  compile_options:
    wio_version: 0.4.0
    default_target: tests

targets:
  tests:
    src: tests
    platform: native
    linker_flags:
    - pthread

dependencies:
  can-packet:
    version: 1.0.1

